sudo: false
language: cpp
compiler:
  - clang
addons:
  apt:
    sources:
    - llvm-toolchain-precise-3.8
    packages:
    - clang-3.8
install:
- "[ $CXX = clang++ ] && export CXX=clang++-3.8 || true"
script:
  - cmake .
  - make
  - aes_exe